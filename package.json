{
  "name": "lightweight-vulnerability-scanner-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack dashboard for lightweight URL and GitHub repository security checks.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "start": "npm run start -w server",
    "build": "npm run build -w client",
    "lint": "npm run lint -w client",
    "scan:demo": "node server/src/demo-scan.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
